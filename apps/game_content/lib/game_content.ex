defmodule GameContent do
  @moduledoc """
  ゲームコンテンツ（静的データ・アセット管理）モジュール。

  利用可能なコンテンツ:
  - `GameContent.VampireSurvivor` — ヴァンパイアサバイバークローン
  - `GameContent.AsteroidArena`   — 小惑星シューター（武器・ボスなし）
  """
end
