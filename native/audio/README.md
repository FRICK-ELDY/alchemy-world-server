# audio

音声再生を担当するクレート。**クライアント側**でデバイス音声出力を行う。

## 責務

- オーディオミキサー、エンジン初期化
- DSP（エフェクト）、立体音響（Spatial Audio）計算
- platform/ で OS ごとの音声ドライバ（rodio 等）を切り替え

## 配置

- **サーバー側**: `nif` が `audio` を参照し、ゲーム内 SE/BGM を再生
- **クライアント側**: `app` が `audio` を参照（将来のローカル再生等）

## 依存

依存なし（基底クレート）。
