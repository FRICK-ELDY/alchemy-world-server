[package]
name = "client"
version = "0.1.0"
edition = "2021"
description = "クライアント共通ライブラリ（client_info, Zenoh 接続）。network + shared の薄いラッパー"

[lib]
crate-type = ["rlib"]

[dependencies]
network = { path = "../network" }
shared = { path = "../shared" }
