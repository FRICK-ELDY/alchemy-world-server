//! 背景クリア色など、レンダラーと NIF の定数で共有する値（SSoT）。

/// Background clear color (dark purple) — R 成分
pub const BG_R: f64 = 0.05;
/// Background clear color — G 成分
pub const BG_G: f64 = 0.02;
/// Background clear color — B 成分
pub const BG_B: f64 = 0.10;
