[package]
name = "client_android"
version = "0.1.0"
edition = "2021"
description = "Android 用クライアント。将来実装予定。"

[[bin]]
name = "client_android"
path = "src/main.rs"

[dependencies]
# TODO: ndk, jni 等で実装
