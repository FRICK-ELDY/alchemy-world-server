[package]
name = "input_openxr"
version = "0.1.0"
edition = "2021"
description = "OpenXR 入力ブリッジ（VR ヘッドセット・コントローラー・トラッカー）"

[features]
default = []
openxr = ["dep:openxr"]

[dependencies]
log = "0.4"
openxr = { version = "0.21", optional = true }
